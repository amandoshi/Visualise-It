/**
 * @const {Number} - maximum possible edge weight for graph
 */
const maxEdgeWeight = 100000;

/**
 * @const {Number} - maximum character length of a node name
 */
const maxNodeNameLength = 20;

/**
 * @const {Number} - maximum number of nodes a graph can contain
 */
const maxNumberOfNodes = 150;
